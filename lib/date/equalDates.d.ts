/**
 * Compare dates by Date-values only (without Time)
 * @return {Boolean} return true if Date-values is the same (including isNaN == isNaN comparison)
 */
export default function equalDates(this: Date, value: Date): boolean;
