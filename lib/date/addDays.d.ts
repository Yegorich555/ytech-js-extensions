/**
 * Add days to date
 * @param {Number} value a number of whole days. Can be negative or positive
 * @return {Date} existed Date with added days
 */
export default function addDays(this: Date, value: number): Date;
